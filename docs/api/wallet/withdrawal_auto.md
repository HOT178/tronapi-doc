# 自动提现

:::tip
所谓 `自动提现`，即当商户账户余额达到指定的 `阈值` 时，系统自动向相关地址进行转账的操作。
:::

商户可根据自身业务诉求，前往后台页面进行自动提现配置：

<img src="/images/withdrawal-auto.png" alt="" width="720"/>

:::tip
商户可同时配置 `提现回调地址`，`自动提现` 操作完成时，会自动向配置的地址发送回调通知。
:::